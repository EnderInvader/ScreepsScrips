//claimState -1: Move to room
//claimState 0: Sign
//claimState 1: Reserve
//claimState 2: Claim

var roleScout = {
    /** @param {Creep} creep **/
    run: function(creep) {
        
	}
};

module.exports = roleScout;